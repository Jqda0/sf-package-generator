name: Publish Extension

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    # Skip if the last commit was a version bump to avoid infinite loops
    if: "!contains(github.event.head_commit.message, 'chore: bump version') && !startsWith(github.event.head_commit.message, 'v')"

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install vsce
        run: npm install -g @vscode/vsce

      - name: Publish with minor version bump
        run: vsce publish minor
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Push version bump and tag
        run: |
          VERSION=$(node -p "require('./package.json').version")
          git tag "v$VERSION" || true
          git push
          git push --tags
